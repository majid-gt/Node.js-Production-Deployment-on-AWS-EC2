# Node.js-Production-Deployment-on-AWS-EC2
Deployed a production-grade Node.js application on AWS EC2 using PM2 for process management and NGINX as a reverse proxy with HTTPS enabled via Let’s Encrypt. Configured custom domain routing through Cloudflare DNS and secured the application with SSL.
